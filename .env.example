# --- Core ---
DATABASE_URL="file:./dev.db"

# --- Admin seed (read once on first DB seed to create the initial admin) ---
ADMIN_EMAIL="admin@wavehack.local"
ADMIN_PASSWORD="ChangeMe!WaveHack2026"
ADMIN_NAME="WaveHack Admin"

# --- NextAuth ---
NEXTAUTH_SECRET="replace-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# --- Email (Resend) — optional; if unset, emails are logged to console ---
RESEND_API_KEY=""
FROM_EMAIL="WaveHack <noreply@wavehack.dev>"

# --- Public site config ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_HACKATHON_NAME="WaveHack"
NEXT_PUBLIC_COUNTDOWN_DATE="2026-08-01T00:00:00Z"
